name: Run Hero editor workspace layout

on:
  push:
    branches: [feat/hero-editor-workspace-layout]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/hero-editor-workspace-layout
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply-hero-editor-workspace-layout.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/SmartScenesManager.tsx src/smart-scenes.css
          git commit -m "feat: reorganize Hero editor workspace"
          git push origin HEAD:feat/hero-editor-workspace-layout
